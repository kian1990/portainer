services:
  registry:
    image: registry:3
    container_name: registry
    ports:
      - "80:5000"
    volumes:
      - registry:/var/lib/registry
    restart: always
    network_mode: "bridge"

volumes:
  registry:
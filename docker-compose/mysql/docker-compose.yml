services:
  mysql:
    image: mysql:8.0.45
    container_name: mysql
    ports:
      - "3306:3306"
    volumes:
      - mysql:/var/lib/mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
    network_mode: "bridge"

volumes:
  mysql:
